[type]
Parser

[grammar]
grammar T;
a : LETTERS* 'd' {print(self._input.getText(), file=self._output)} ;
// These are actually not escaped -- Java passes the
// raw unescaped Unicode values to the grammar compiler.
LETTERS : ('a'|'à'..'å');

[start]
a

[input]
aáäáâåd

[output]
"""aáäáâåd
"""

