[type]
Parser

[grammar]
grammar T;
a : LETTERS {print(self._input.getText(), file=self._output)} ;
// These are actually not escaped -- Java passes the
// raw unescaped Unicode values to the grammar compiler.
LETTERS : ('a'|'ä'|'亜'|'あ')* 'c';

[start]
a

[input]
aäあ亜c

[output]
"""aäあ亜c
"""

