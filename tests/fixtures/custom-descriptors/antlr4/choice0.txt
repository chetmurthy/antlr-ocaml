[type]
Parser

[grammar]
grammar choice1;

@lexer::members {

def nextToken(self):
    import Trace
    orig = Trace.disable()
    rv = super().nextToken()
    Trace.restore(orig)
    return rv

}
top : (( 'a' B ) | ('a' C)) EOF ;

B : 'b' ;
C : 'c' ;

[start]
top

[input]
"""ab"""
[output]
[output type=lexer-only-named-types]
[input name=B]
"""ac"""
[output name=B]
[output name=B type=lexer-only-named-types]
